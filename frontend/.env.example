# El backend ya no es un servidor HTTP: los motores corren en el navegador con
# Pyodide. No hace falta ninguna URL de API.
#
# (Opcional) Sobrescribe el origen del runtime de Pyodide — por defecto se usa
# la CDN de jsDelivr. Útil para autoalojarlo o usar otra CDN.
# VITE_PYODIDE_INDEX_URL=https://cdn.jsdelivr.net/pyodide/v0.27.2/full/
